Todo
----

** What if a CD server doesn't have access to Master? Do we need a config setting to say "which db should I read config from",
   and separate config includes for CM/CD and CD-Only installs?

** Command & buttons to triger regen without publish?

** does it need an "include in sitemap" field in the template?

** Perf improvement - is it possible to do partial rebuilds of the sitemap file?